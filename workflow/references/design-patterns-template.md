# {PROJECT_NAME} — Patterns & Conventions

THE HOW, repeated. Reusable implementation patterns and idioms this codebase
uses, so new work stays consistent with existing work. Part of `design/`.
Status-free. Complements `spec/standards.md` (which sets the rules) by showing
the concrete, in-repo way those rules are met.

---

## Pattern: [name, e.g. "Confidence gate + HITL suspend"]

**Problem:** [The recurring situation this pattern addresses.]

**Solution:** [The shape of the solution as used here.]

**Example:** [path to a canonical instance in the codebase]

**When to use / avoid:** ...

---

## Pattern: [name]

...

---

## Anti-patterns

- [Things done before that bit us — cross-link `DECISIONS.md` gotchas.]
